{
    "version": "2.0.0",
    "tasks": [
        { 
            "type": "shell",
            "label": "compile-server",
            "command": "npm run compile",
            "options": {
                "cwd": "${workspaceFolder}/backend"
            }
        }
    ]
}